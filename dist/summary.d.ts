import React from "react";
declare type Props = {
    id?: string;
    children: React.ReactNode;
    className?: string | undefined;
};
export default function Summary({ id, children, className }: Props): JSX.Element;
export {};
